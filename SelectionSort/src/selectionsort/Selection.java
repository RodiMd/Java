/*
 * Author: Rodi Murad
 * Subject: Selection Sort
 */
package selectionsort;

public class Selection {
    
    public static <T extends Comparable<T>> int Sort(T[] array, int stScan, int index, int minIndex, int minValue, T key){
       
        int end = 0;
        
        for (stScan = 0; stScan < array.length -1; stScan++){
            
        
        
        
        
        return end;
    }
    
    //int stScan, index, minIndex, minValue;
    
        return end;
    }
}
